# Shell:

- it just interprets the commands you type and execute them.

## REPL

- Read {read the commands u type}
- Evaluate {evaluate those cmds, by running other programs on compt}
- Print {print the o/p of those cmds}
- Loop {gives a new prompt for another cmd and repeat}
