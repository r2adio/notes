# QUICK CONFIG:

git comes with a configuration both at the global and the repo level.

## git configuration setup:

```shell
git config --add --global user.name "github_username"
git config --add --global user.email "email@example.com"
```

## to set default branch

```shell
git config --add --global init.defaultBranch master
```
